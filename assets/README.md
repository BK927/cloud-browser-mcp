# Cloud Browser MCP icon

Created on 2026-09-08 at the user's request with the built-in ImageGen tool
(single image, no reference input).

- `cloud-browser-icon-source-20260908.png`: retained original generation.
- `cloud-browser-icon-256-20260908.png`: ChatGPT upload asset, 256×256 RGBA PNG,
  2,297 bytes. Resized and palette-encoded with Sharp (16 colours, no dithering);
  inspected visually after conversion. No existing project asset was replaced.

## Final generation prompt

Create one original app icon for 'Cloud Browser MCP', a remote browser automation
service. Logo-brand use case. A compact, beautifully balanced flat vector-like
bitmap symbol: one simplified rounded browser window enclosing a fluffy white
cloud, with a small clear pointer arrow at the lower right to suggest remote
browser control. Indigo and bright sky-blue with white, dark navy contrast.
Friendly technical, premium minimal app identity. Square composition, centered
with generous 15 percent safe margins, strong silhouette legible at 32px, very
few solid color regions, crisp clean edges, no fine detail, no gradients or
texture, no lettering, no numbers, no brand logos, no watermark, no mockup.
Solid deep indigo background edge to edge. Single icon only, not an icon sheet.
Intended final PNG is 256x256 and under 10KB, so keep geometry and palette
especially simple.

The selected generated image has transparency outside the browser symbol;
the upload asset preserves it. This is an app-registration asset, not a new
browser container image or a change to the service's security settings.
